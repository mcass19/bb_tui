defmodule BB.TUI.Test.Endpoint do
  @moduledoc false

  # Minimal endpoint for Phoenix.LiveViewTest — `live_isolated/3` needs a
  # running endpoint but no router, plugs, or HTTP server.
  use Phoenix.Endpoint, otp_app: :bb_tui
end
